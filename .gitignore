*.iml
.gradle
**/.gradle/
**/.kotlin/
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
**/build/
/captures
.externalNativeBuild
.cxx
local.properties

# Secrets / signing — debug keystore explicit istisna (commit'e izin ver)
# NOT: gitignore satır-sonu yorumu desteklemez; yorum kendi satırında olmalı,
# aksi halde pattern yorumu da kapsar ve hiçbir şeyle eşleşmez.
.env
.env.*
!*.example
secrets/
*.jks
*.keystore
# stable debug signing — repo'da kalır (deterministic build)
!app/mimir-debug.keystore
# release imzalama parametreleri — asla commit edilmez
signing.properties
# devs için override slot
google-services.local.json

# Security Audit 2026-05-13 23:30 (UTC+3)

**Özet:** ✅ Temiz — 0 kritik / 0 orta / 8 info satır

## 🔴 Kritik
_yok_

## 🟡 Orta
_yok_

## 🟢 Info

### Dependencies
- **Backend (mimir-api):** 0 vulnerable package — `dotnet list package --vulnerable --include-transitive` sonucu temiz (NuGet + Microsoft SDK source'ları)
- **Mobile (mimir-mobile):** gradle scan deferred — sonraki run'da `:app:dependencies` taraması eklenecek

### Secret Leak (son 48 saat commit)
- Tüm regex pattern'leri (api-key, secret, password, JWT, AWS, GCP, Slack, private key) **0 match**
- Son commit'ler: ADR-021 (doc-only), TEAM_NOTES iOS disipline, Sprint #13 stabilization — yalnızca doc + 2 backend fix

### Code-Level
- **Auth coverage:** Son 24 saat Controller değişikliği yok
- **SQL raw query:** `FromSqlRaw`/`ExecuteSqlRaw` kaynak kodda yok (sadece EFCore binary match — false positive)
- **HTTP cleartext (mobile):** `http://` referansları sadece **XML namespace URI'leri** (`xmlns:android="http://schemas.android.com/..."` — Google resmi schema, gerçek network call değil). Filtreyi `http://` + `host` pattern'ine indirgemek için routine'i revize et.
- **Log leak:** `_logger.Log*.*password|token` match'leri kontekst-güvenli — sadece `{UserId}`, `{Username}`, `{Count}` ve `Truncate(token)` (ilk 16 char) log'lanıyor. Plain password/token leak yok.

### VPS Operational
| Metric | Değer | Durum |
|---|---|---|
| Disk `/` | 34/75 GB (**%48**) | ✅ |
| RAM | 1.0/3.7 GB (**%27**) | ✅ |
| `mimir-web` | Up 27 dk (healthy) | ✅ |
| `mimir-db` | Up 4 gün (healthy) | ✅ |
| `mimir-redis` | Up 3 gün (healthy) | ✅ |
| `mimir-coturn` | Up 2 gün | ✅ |
| Error count 24h | **0** | ✅ |

### UFW (firewall)
Beklenen 10 kural — sadece 22/80/443 (SSH/web) + 3478/5349/49152-49200 (voice call). Beklenmedik açık port yok.

### Configuration Drift
- `MIN_APP_VERSION_ANDROID=0.9.2` = `LATEST_APP_VERSION_ANDROID=0.9.2` → **info** (force-update sürekli aktif — Sprint #12'den beri kasıtlı; eski APK'lar 426 alır)
- `image: *:latest` tag yok ✅ (anti-pattern 33 disiplin)
- TURN_SECRET, JWT_KEY: env-only, source'ta görünmüyor ✅

### Certificate Expiry
- `aykutonpc.com` Let's Encrypt cert: `notAfter=Jul 18 2026` → **~66 gün kaldı** ✅ (eşik 30 gün)

## 📊 Önceki Rapor ile Karşılaştırma
İlk rapor — baseline.

---

**Routine version:** `nightly-security.md` v1 (ilk manuel çalıştırma)
**Çalıştırma süresi:** ~90 saniye (paralel komutlar + SSH SCH check)

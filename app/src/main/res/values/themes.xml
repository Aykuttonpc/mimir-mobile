<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Compose Material3 zaten runtime theme'i kontrol ediyor; bu sadece app initial splash + system bars için placeholder. -->
    <style name="Theme.Mimir" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="s">@android:color/black</item>
    </style>
</resources>
